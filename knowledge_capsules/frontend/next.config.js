/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Включает строгий режим React для выявления потенциальных проблем в приложении
  // Сюда можно будет добавлять другие конфигурации по мере роста проекта
  // Например:
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
